[project]
name = "quam-libs"
version = "0.0.0"
requires-python = ">=3.10,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["quam_libs"]